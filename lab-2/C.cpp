#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    while(t--){
        int d;
        cin>>d;
        vector<int> nums(d);
        for(int i=0; i<d; i++) cin>>nums[i];
        
    }
    return 0;
}